Project Invoice Hours Module
############################

The Project Invoice Hours module adds a new invoice method on projects named
On Hours for which the invoices are created based on the hours in field *Hours*
for all children works in state *Done*.
